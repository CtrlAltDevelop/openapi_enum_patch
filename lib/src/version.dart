/// The published version of this package, printed by `--version`.
///
/// Kept here rather than in `bin/` so the CLI and any library consumer read
/// the same constant; `test/version_test.dart` holds it to `pubspec.yaml`.
const packageVersion = '1.3.0';
